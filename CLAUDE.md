# CLAUDE.md

This file provides guidance to Claude Code when working with this codebase.

## Coding Standards

- **Always use type hints** for all function signatures, method parameters, and return values
- Example: `def greet(name: str) -> str:`
- Avoid using `Any` or untyped code when possible

## Project Overview

This is a Python calculator application.
