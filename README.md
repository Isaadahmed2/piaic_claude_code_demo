# Creating calculator using claude code
*created an arithematic calculator using claude code*
## Installation of claude code router

to install cluade  code ccr use the following line 

`npm install -g @musitudio/claude-code-router`

**Setup Open router**

- Add provider
- select free model from openrouter
- Add configuration for thing reasoning model

**Pre requisites for claude core router**

- Node should be installed
- git bash should be installed 
- calude code cli should be installed

using command 
`npm install -g @anthropic-ai/claude-code`





